
####################
# Project Metadata #
####################
[project]
authors = [{ name = "Cody Fincher", email = "cody@litestar.dev" }]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Web Environment",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Natural Language :: English",
  "Operating System :: OS Independent",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Software Development",
  "Typing :: Typed",
]
dependencies = ["litestar>=2.24.0", "typing_extensions>=4.0.0"]
description = "Task queues, workers, schedules, and backend integrations for Litestar"
keywords = [
  "advanced-alchemy",
  "background-jobs",
  "cloud-run",
  "litestar",
  "queue",
  "redis",
  "scheduled-tasks",
  "sqlspec",
  "task-queue",
  "tasks",
  "valkey",
  "workers",
]
license = { text = "MIT" }
name = "litestar-queues"
readme = "README.md"
requires-python = ">=3.10"
version = "0.11.0"

[project.urls]
Changelog = "https://cofin.github.io/litestar-queues/changelog"
Discord = "https://discord.gg/litestar-919193495116337154"
Documentation = "https://cofin.github.io/litestar-queues/"
Homepage = "https://cofin.github.io/litestar-queues/"
Issue = "https://github.com/cofin/litestar-queues/issues/"
Source = "https://github.com/cofin/litestar-queues"

[project.entry-points."litestar.commands"]
queues = "litestar_queues._cli:queues_group"

[project.optional-dependencies]
advanced-alchemy = ["advanced-alchemy>=1.10.0"]
cloud-tasks = ["google-cloud-tasks>=2.23.0"]
cloudrun = ["google-cloud-run>=0.10.0"]
kafka = ["aiokafka>=0.14.0"]
pubsub = ["google-cloud-pubsub>=2.39.0"]
rabbitmq = ["aio-pika>=10.0.1; python_version >= '3.11'"]
sqs = ["aiobotocore>=3.8.0"]
otel = ["opentelemetry-api", "opentelemetry-sdk"]
prometheus = ["prometheus-client"]
redis = ["redis>=5.0.0"]
sqlspec = ["sqlspec>=0.58.0"]
valkey = ["valkey>=6.0.0"]

######################
# Build & Versioning #
######################
[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.hatch.build.targets.sdist]
dev-mode-dirs = ["."]
exclude = ["/.github", "/docs"]

[tool.hatch.build.targets.wheel]
packages = ["src/litestar_queues"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.bumpversion]
allow_dirty = true
commit = false
commit_args = "--no-verify"
current_version = "0.11.0"
ignore_missing_files = false
ignore_missing_version = false
message = "chore(release): bump to v{new_version}"
parse = """(?x)
    (?P<major>\\d+)\\.(?P<minor>\\d+)\\.(?P<patch>\\d+)
    (-(?P<pre>alpha|beta|rc)\\.(?P<pre_n>\\d+))?
"""
regex = false
replace = "{new_version}"
search = "{current_version}"
serialize = ["{major}.{minor}.{patch}", "{major}.{minor}.{patch}-{pre}.{pre_n}"]
sign_tags = false
tag = false
tag_message = "chore(release): v{new_version}"
tag_name = "v{new_version}"

[tool.bumpversion.parts.pre]
first_value = "stable"
optional_value = "stable"
values = ["alpha", "beta", "rc", "stable"]

[tool.bumpversion.parts.pre_n]
first_value = "1"

[[tool.bumpversion.files]]
filename = "pyproject.toml"
replace = 'version = "{new_version}"'
search = 'version = "{current_version}"'

[[tool.bumpversion.files]]
filename = "uv.lock"
replace = """
name = "litestar-queues"
version = "{new_version}"
"""
search = """
name = "litestar-queues"
version = "{current_version}"
"""

#####################
# Environment Setup #
#####################
[dependency-groups]
benchmark-backends = [
  "advanced-alchemy>=1.10.0",
  "asyncpg>=0.30.0",
  "google-cloud-run>=0.10.0",
  "google-cloud-tasks>=2.23.0",
  "google-cloud-pubsub>=2.39.0",
  "hiredis",
  "libvalkey>=4.0.1",
  "psutil",
  "psycopg[binary,pool]>=3.2.0",
  "redis>=5.0.0",
  "sqlalchemy>=2.0.0",
  "sqlspec[performance]>=0.58.0",
  "valkey>=6.0.0",
]
benchmark-frameworks = ["arq", "celery", "dramatiq", "rq", "saq", "taskiq", "taskiq-redis"]
benchmarks = [{ include-group = "benchmark-backends" }, { include-group = "benchmark-frameworks" }]
build = ["bump-my-version"]
dev = [
  { include-group = "lint" },
  { include-group = "tests" },
  { include-group = "docs" },
  { include-group = "build" },
  { include-group = "examples" },
]
docs = [
  "auto-pytabs[sphinx]>=0.5.0",
  "litestar-vite",
  "shibuya",
  "sphinx-autobuild>=2021.3.14",
  "sphinx-copybutton>=0.5.2",
  "sphinx-design>=0.5.0",
  "sphinx-paramlinks>=0.6.0",
  "sphinx-togglebutton>=0.3.2",
  "sphinx>=7.1.2",
  "sphinxcontrib-mermaid>=0.9.2",
]
e2e = ["playwright>=1.50.0", "pytest-playwright>=0.7.0"]
examples = ["aiosqlite>=0.20.0", "jinja2>=3.1.0", "litestar-vite>=0.26.1"]
lint = ["mypy", "pyright", "ruff", "slotscheck"]
test-adapters = [
  "adbc-driver-sqlite",
  "advanced-alchemy>=1.10.0",
  "google-cloud-spanner>=3.47.0",
  "hiredis",
  "libvalkey>=4.0.1",
  "litestar-granian>=0.16.0b2",
  "numpy",
  "opentelemetry-api",
  "opentelemetry-sdk",
  "prometheus-client",
  "psutil",
  "pyarrow",
  "aio-pika>=10.0.1; python_version >= '3.11'",
  "pytest-databases[postgres,mysql,mssql,oracle,redis,valkey,cockroachdb,spanner]>=0.19.0",
  "redis>=5.0.0",
  "sqlspec[adbc,aiosqlite,oracledb,aiomysql,psycopg,asyncpg,duckdb,psqlpy,asyncmy,arrow-odbc,cockroachdb,pymysql,spanner,litestar,mysql-connector,pymssql,mssql-python]>=0.58.0",
  "valkey>=6.0.0",
]
test-core = [
  "anyio",
  "coverage[toml]",
  "litestar[standard,attrs,pydantic]",
  "pydantic>=2.0.0",
  "pytest",
  "pytest-cov",
  "pytest-mock",
  "pytest-sugar",
  "pytest-timeout",
  "pytest-xdist",
  "tomli; python_version < '3.11'",
]
tests = [{ include-group = "test-core" }, { include-group = "test-adapters" }]

#####################
# Testing           #
#####################
[tool.pytest.ini_options]
addopts = "-ra -q --dist loadgroup"
filterwarnings = [
  "ignore::DeprecationWarning:pkg_resources.*",
  "ignore::DeprecationWarning:xdist.*",
  "ignore::DeprecationWarning:importlib._bootstrap",
  "ignore:the \\(type, exc, tb\\) signature of throw\\(\\) is deprecated, use the single-arg signature instead\\.:DeprecationWarning:asyncpg\\.connection",
  "ignore:the \\(type, exc, tb\\) signature of throw\\(\\) is deprecated, use the single-arg signature instead\\.:DeprecationWarning:asyncio\\.locks",
]
markers = [
  "e2e: end-to-end tests that start real example processes and Chromium",
  "topology: cross-process queue and Channels topology tests",
]
testpaths = ["src/tests"]

[tool.coverage.report]
exclude_lines = [
  "pragma: no cover",
  "def __repr__",
  "if self\\.debug",
  "raise AssertionError",
  "raise NotImplementedError",
  "if 0:",
  "if __name__ == .__main__.:",
  "if TYPE_CHECKING:",
  'class .*\bProtocol\):',
  '@(abc\.)?abstractmethod',
]

[tool.coverage.run]
branch = true
concurrency = ["multiprocessing"]
disable_warnings = ["no-data-collected", "module-not-measured", "module-not-imported"]
omit = ["src/tests/*"]
parallel = true
source = ["src/litestar_queues"]

#####################
# Tools             #
#####################
[tool.uv]
exclude-newer = "7 days"

[tool.uv.exclude-newer-package]
advanced-alchemy = false
litestar = false
litestar-asyncapi = false
litestar-asyncpg = false
litestar-autowire = false
litestar-email = false
litestar-granian = false
litestar-htmx = false
litestar-mcp = false
litestar-oracledb = false
litestar-queues = false
litestar-saq = false
litestar-security = false
litestar-vite = false
polyfactory = false
pytest-databases = false
sqlglot = false
sqlglotc = false
sqlspec = false

[tool.ruff]
exclude = [".venv", "node_modules", "tmp", ".tmp", ".bugs", "docs/_build"]
include = [
  "pyproject.toml",
  "src/litestar_queues/**/*.py",
  "src/litestar_queues/**/*.pyi",
  "src/tests/**/*.py",
  "src/tests/**/*.pyi",
  "docs/**/*.py",
  "tools/**/*.py",
]
line-length = 120
src = ["src/litestar_queues", "src/tests", "docs", "tools"]
target-version = "py310"

[tool.ruff.format]
docstring-code-format = true
docstring-code-line-length = 60
preview = true
skip-magic-trailing-comma = true

[tool.ruff.lint]
explicit-preview-rules = true
extend-safe-fixes = ["TC"]
extend-select = ["ASYNC119", "DOC402"]
fixable = ["ALL"]
ignore = [
  "A003",
  "A005",
  "ANN401",
  "ARG001",
  "ARG002",
  "B010",
  "COM812",
  "CPY001",
  "D100",
  "D101",
  "D102",
  "D103",
  "D104",
  "D105",
  "D106",
  "D107",
  "D202",
  "D205",
  "D415",
  "DOC201",
  "E501",
  "FA100",
  "FBT001",
  "FBT002",
  "FBT003",
  "FIX002",
  "ISC001",
  "PGH003",
  "PLC0415",
  "PLR0913",
  "PLW0717",
  "PLW2901",
  "RUF012",
  "RUF029",
  "TD002",
  "TD003",
  "UP037",
]
preview = true
select = ["ALL"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.isort]
known-first-party = ["litestar_queues", "tests"]
split-on-trailing-comma = false

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
"docs/**/*.*" = ["A", "B", "D", "DTZ", "ERA", "RET", "S", "TC"]
"src/litestar_queues/_cli.py" = ["BLE001"]
"src/litestar_queues/_typing.py" = ["PLR0917"]
"src/litestar_queues/backends/advanced_alchemy/mixins.py" = ["N805", "PLW3201"]
"src/litestar_queues/backends/sqlspec/backend.py" = ["BLE001"]
"src/litestar_queues/backends/sqlspec/stores/**/*.*" = ["SLF001"]
"src/litestar_queues/execution/cloudrun/backend.py" = ["BLE001"]
"src/litestar_queues/service.py" = ["BLE001"]
"src/tests/**/*" = [
  "ANN001",
  "ANN201",
  "C901",
  "D103",
  "D104",
  "ERA001",
  "FA102",
  "PLR2004",
  "PLR6301",
  "PT",
  "S",
  "S101",
  "S404",
  "SLF001",
  "T201",
  "TID252",
]
"tools/**/*.*" = ["BLE001", "D", "DOC502", "S310", "S404", "S603", "S607", "T201"]

[tool.ruff.lint.mccabe]
max-complexity = 12

[tool.ruff.lint.pylint]
max-bool-expr = 10
max-branches = 20
max-locals = 20
max-nested-blocks = 7
max-public-methods = 30
max-returns = 15

[tool.ruff.lint.pep8-naming]
classmethod-decorators = [
  "classmethod",
  "sqlalchemy.ext.declarative.declared_attr",
  "sqlalchemy.orm.declared_attr.directive",
  "sqlalchemy.orm.declared_attr",
]

[tool.slotscheck]
strict-imports = false

[tool.mypy]
disallow_any_generics = false
disallow_untyped_decorators = true
implicit_reexport = false
mypy_path = "src"
packages = ["litestar_queues", "tests"]
show_error_codes = true
strict = true
warn_redundant_casts = true
warn_return_any = true
warn_unreachable = true
warn_unused_configs = true
warn_unused_ignores = true

[[tool.mypy.overrides]]
ignore_missing_imports = true
module = ["google.cloud.*"]

[tool.codespell]
skip = "uv.lock"

[tool.pyright]
include = ["src/litestar_queues"]
pythonVersion = "3.10"
reportAssignmentType = "warning"
reportOptionalMemberAccess = "warning"
typeCheckingMode = "standard"
